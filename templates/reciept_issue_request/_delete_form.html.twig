<form method="post" action="{{ path('app_reciept_issue_request_delete', {'id': reciept_issue_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reciept_issue_request.id) }}">
    <button class="btn">Delete</button>
</form>
